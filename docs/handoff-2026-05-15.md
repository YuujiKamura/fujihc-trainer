---
session_handoff
date: 2026-05-15
reason: GPU 現物確認 + ファン交換検討のため作業中断、 次セッションで再開
last_commit: 2c1e116 (phase 1 follow-up: GPX export, MapLibre viewer variant, test suite)
prev_commit: 1b3442c (phase 1: trainer-bridge + viewer WebSocket + CSV ride log)
---

# fujihc-trainer 作業ハンドオフ

## 中断理由

ride 中の GPU 負荷検証中、 GPU ファンの音が変だと user 判断 → 物理的に PC を開けて GPU 現物確認 + ファン交換検討。 PC シャットダウンするため作業内容を保存。

## 現在地

### コードベース

- リポ: `~/fujihc-trainer/` (ローカルのみ、 remote 無し)
- 直前 commit: 上記 frontmatter
- 内容: Phase 1 (= BLE trainer + WebSocket viewer + CSV ride log) + 当日追加 (GPX 出力 / MapLibre 試験版 / pairing UI / 全 test)
- test: `pytest -q` → **31 passed, 4 skipped**、 全 green

### 文脈 (= 何故ここに居るか)

1. 今日 1 日で Phase 1 を完走、 viewer は Cesium 版 (本流) と MapLibre 試験版の 2 系統が並走
2. user 判断で **Cesium 版は OSS にできない → 凍結** (brief 12)
3. 7 軸 audit Round 1 (= 現状コードに対する audit) で **セキュリティ境界軸 LOAD-BEARING NG** が 2 件: `prefetchTilesAlongCourse` が OSM Tile Usage Policy + GSI 地理院利用規約に違反 (= 最大 2700 OSM + 450 GSI タイルを rate limit なし並列 fetch)
4. 修正方針確定: 短期 (= 関数停止) + 中期 (= ローカル SQLite に PMTiles + GSI dem を 1 回だけ計画的に DL、 走行時は外部 fetch ゼロ)
5. brief 12-19 を起草 (= 9 brief)、 Round 2 audit 完了 (= LOAD-BEARING NG 残あり、 主に数字根拠 / schema_version / 中間状態保証)
6. brief 12-19 を **redraft 完了** (= Round 2 の指摘を全て織込み済)、 これが現状

### user 追加要望 (中断直前)

1. **zoom 1 段固定** + **コース付近限定** で配信サーバ負担と GPU 負荷を減らす方針 → brief 14 の `OSM_VECTOR_ZOOMS = [17]` 単一化 (= 300 タイル / 15 MB、 brief 14 現案 1185 タイル / 59 MB の 1/4)
2. **計測しながら進める** 方針確認 → 次セッションで「計測 substrate」brief 20 を新規追加予定 (= nvidia-smi polling + FPS HUD + tile fetch カウンタ)
3. **GPU ファン交換**: 物理確認のため中断

### GPU baseline (idle、 当日 23:30 頃計測)

- model: NVIDIA GeForce GTX 1650
- temperature: 47℃
- fan: 40%
- utilization: 17%
- memory: 1317 / 4096 MiB
- power: 12.17W / 75W

数値は idle 正常、 ファン音の異常は数値では出ない (= 物理 / 周波数特性の問題と推定)。 交換後の baseline 取り直しが要る。

## 次セッションで再開する起点

### 即時着手 (PC 起動 + ファン交換完了後)

1. **GPU baseline 再計測** (= ファン交換が効いたか確認):
   ```bash
   nvidia-smi --query-gpu=name,temperature.gpu,fan.speed,utilization.gpu,memory.used,power.draw --format=csv
   ```
   旧 baseline (47℃ / 40% fan / 17% util / 1317 MiB / 12W) と比較
2. **作業ディレクトリ**: `cd ~/fujihc-trainer/`
3. **test 確認**: `pytest -q` で 31 passed / 4 skipped 維持を確認

### brief 着手順 (= redraft 済 9 brief から)

依存関係:
```
brief 12 (Cesium 凍結) ────────────┐
brief 14 (DB schema)  ─────┬───────┤
brief 18 (JS test 基盤) ───┤       │
                           ├─► brief 13 (prefetch 停止) ──┐
                           ├─► brief 15 (GSI DL)          │
                           ├─► brief 16 (OSM PMTiles)     │
                           ├─► brief 17a (tile_server) ───┤
                           └─► brief 17b (viewer 経路) ───┘
                                                          ▼
                                                    brief 19 (viewer 統合層)
```

着手は **brief 12 (Cesium 凍結) と brief 14 (DB schema)** が独立先行可能。 「計測 substrate」brief 20 を先に書くなら brief 14 と並行。

### 残作業 (= 次セッションで書く)

- **brief 20: 計測 substrate** ── GPU polling script + FPS HUD lib + tile fetch カウンタ
- brief 14 の `OSM_VECTOR_ZOOMS` を `[17]` 単一化 + brief 16 連動 (= user 要望反映)
- brief 16 の corridor を 14 の中央定数経由に統一済 (= redraft で実施)

## ファイル location

### brief 群

- `~/.agents/scratch/fujihc-trainer-project/briefs/`
  - `12-cesium-freeze.md` 〜 `19-viewer-integration-layer.md` (= 9 brief)
  - `archived/17-tile-server-endpoint-superseded-by-17a-17b.md` (= 古い brief 17、 分割で superseded)

### audit 履歴 (= drift catalog)

- `~/.agents/state/fujihc-trainer/audit-drift-catalog.md` (= Round 1 NG 16 件 + 共通 pattern 5 種、 Round 2 で参照済)

### Round 1 audit 出力 (= 現状コードへの最初の audit)

- 集約は session 内のみ、 個別 subagent 出力は jsonl に保存されてる
- 主結論: prefetch policy 違反 LOAD-BEARING / JS test 基盤無し CRITICAL / 2 viewer 並走

### Round 2 audit 出力 (= brief 12-18 への 2 回目 audit)

- 集約は session 内のみ、 主結論: 数字根拠不在 / schema_version 欠落 / bridge.py 肥大 / 中間状態保証なし
- → brief 12-19 を Round 2 受けて redraft 済 (= 現状ファイル)

## ハマる罠 (次セッションが拾う時)

- `~/.agents/scratch/fujihc-trainer-project/briefs/` を見ると brief 17 が **17a と 17b に分割済**、 旧 17 は `archived/` 配下、 file name 順で見ると 17a → 17b → 17 (= 18 直前) の順だが 17a/17b が正で 17 は使うな
- redraft で frontmatter に `revised: 2026-05-14` を入れた、 これがある brief は Round 2 反映済、 無い brief (= 12 / 17a / 17b / 19) は新規または初回完成版で revised の必要無し
- `brief 13` と `brief 17b` の dependency が `brief 18` に変わってる (= JS test 基盤無いと load-bearing 変更を test 無しで landing する事故になる)、 順序を守れ
- user 要望「zoom 1 段固定」は brief 14 の `OSM_VECTOR_ZOOMS` を `[17]` に変えるだけだが、 brief 16 の抽出と brief 17b の MapLibre style (overzoom 前提) と連動する、 一括で touch
- GPU 計測の baseline は 47℃ idle、 ファン交換後にこれが変わってる可能性、 比較対象として記録済 (本ファイル「GPU baseline」section)

## このセッションの session_id

(= ~/.claude/projects/C--Users-yuuji/ の最新 jsonl が本セッション、 vault には ~/user-context-vault/voice/sessions/ に hook 経由で記録)

## 物理確認ポイント (ファン交換時の備忘)

- GTX 1650: シングルファンモデル、 4GB GDDR5/GDDR6 (= 4096 MiB 表示)
- 当日 idle で 47℃ / fan 40% は数値上は正常域、 ただし「音が変」を user が体感
- 確認すべき:
  - ファン軸受のグリス枯れ (= 高音域の擦れ音)
  - ファンブレードの埃詰まり (= 低音域の不均衡振動)
  - PCIe 補助電源コネクタ確認 (= GTX 1650 は通常 75W 以下で補助無しモデル多い)
  - ヒートシンクとファン取付の緩み
- 交換するなら同径同厚のリプレースメントファン (= 通常 65-92mm)

---

セッション再開時はこのファイルを Read してから着手しろ。
